Start by installing DynamoDB locally and project dependencies with `npm install`, then run commands below in your terminal

```sh
node import-model.js
node create-leaderboard.js
node scan-leaderboard.js
```